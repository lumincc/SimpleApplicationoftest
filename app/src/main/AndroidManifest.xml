<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.simpleapplicationoftest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleApplicationOfTest">
        <activity
            android:name=".ThirdActivity"
            android:exported="false" />
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="http"/>
            </intent-filter>
        <activity
            android:name=".SecondActivity"
            android:exported="true" /> <!-- <intent-filter> -->
        <!-- &lt;!&ndash;                <action android:name="com.example.activitytest.ACTION_START"/>&ndash;&gt; -->
        <!-- &lt;!&ndash;                <category android:name="android.intent.category.DEFAULT"/>&ndash;&gt; -->
        <!-- &lt;!&ndash;                <category android:name="com.example.activitytest.MY_CATEGORY"/>&ndash;&gt; -->
        <!-- </intent-filter> -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>